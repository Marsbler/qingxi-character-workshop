# Application deps for Qingxi Character Workshop.
# IMPORTANT (Radeon / ROCm):
#   1) Install ROCm PyTorch FIRST (image preinstall or pytorch.org rocm wheels).
#   2) Then: pip install -r requirements.txt
#   3) NEVER install default PyPI "torch" (often +cu* CUDA builds).
#   accelerate is listed with a note — install after ROCm torch so pip does not
#   pull NVIDIA wheels. Prefer: pip install accelerate --no-deps
#   after the rest, OR install requirements then immediately reinstall ROCm torch.

gradio>=4.44.0
pydantic>=2.7.0
pyyaml>=6.0.1
Pillow>=10.4.0
numpy>=1.26.0
huggingface_hub>=0.24.0
transformers>=4.44.0
diffusers>=0.30.0
safetensors>=0.4.0
sentencepiece>=0.2.0
protobuf>=4.25.0
# Install AFTER ROCm torch is confirmed working:
#   python3 -m pip install 'accelerate>=0.33.0' --no-deps
#   python3 -m pip install psutil packaging pyyaml  # accelerate runtime deps if missing
